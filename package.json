{
  "name": "dominus",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",                           
    "build": "vite build --config vite.config.js",  
    "build:gh": "vite build --config vite.config.gh.js",  
    "build:npm": "vite build --config vite.config.npm.js",  
    "deploy": "npm run build:gh && gh-pages -d dist",  
    "prepare": "npm run build:npm"                    
  },
  "devDependencies": {
    "vite": "^5.4.5",
    "gh-pages": "^4.0.0"
  }
}
